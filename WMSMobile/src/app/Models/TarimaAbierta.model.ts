export interface TarimaAbierta {
  operacion: number;
  planta_id: string;
  orden_compra_recepcion_id: number,
  secuencia_id: number,
  tarima_id: string,
  estatus: number,
  usuario_asignacion_id: string
}
